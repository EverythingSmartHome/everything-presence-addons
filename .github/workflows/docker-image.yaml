name: Build and Push Docker Image
permissions:
  contents: read
  packages: read
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-pr-matrix:
    name: "Build Docker Image (PR) (${{ matrix.arch }})"
    if: github.event_name != 'push'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            arch: amd64
            runner: ubuntu-latest
            build_from: ghcr.io/hassio-addons/base:19.0.0
          - platform: linux/arm64
            arch: arm64
            runner: ubuntu-24.04-arm
            build_from: ghcr.io/hassio-addons/base:19.0.0
          - platform: linux/arm/v7
            arch: armv7
            runner: ubuntu-latest
            build_from: ghcr.io/home-assistant/armv7-base:latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract version from config.yaml
        run: |
          VERSION=$(grep '^version:' everything-presence-mmwave-configurator/config.yaml | awk '{print $2}' | tr -d '"')
          echo "Extracted version: $VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Set Image Tags
        run: |
          echo "STANDALONE_IMAGE_TAG=test/everything-presence-mmwave-configurator:${{ env.VERSION }}-${{ matrix.arch }}" >> $GITHUB_ENV
          echo "ADDON_IMAGE_TAG=test/everything-presence-mmwave-configurator:addon-${{ env.VERSION }}-${{ matrix.arch }}" >> $GITHUB_ENV

      - name: Build standalone image
        uses: docker/build-push-action@v4
        with:
          context: ./everything-presence-mmwave-configurator
          target: standalone
          platforms: ${{ matrix.platform }}
          push: false
          load: false
          tags: |
            ${{ env.STANDALONE_IMAGE_TAG }}

      - name: Build add-on image
        uses: docker/build-push-action@v4
        with:
          context: ./everything-presence-mmwave-configurator
          target: addon
          platforms: ${{ matrix.platform }}
          push: false
          load: false
          build-args: |
            BUILD_FROM=${{ matrix.build_from }}
          tags: |
            ${{ env.ADDON_IMAGE_TAG }}

  build-pr:
    name: Build Docker Images (PR)
    if: ${{ always() && github.event_name != 'push' }}
    needs: build-pr-matrix
    runs-on: ubuntu-latest
    steps:
      - name: Check matrix result
        run: |
          if [ "${{ needs.build-pr-matrix.result }}" != "success" ]; then
            echo "One or more matrix builds failed: ${{ needs.build-pr-matrix.result }}"
            exit 1
          fi
          echo "All matrix builds passed"

  build-push-standalone:
    name: Build and Push Standalone Image
    if: github.event_name == 'push'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            arch: amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            arch: arm64
            runner: ubuntu-24.04-arm
          - platform: linux/arm/v7
            arch: armv7
            runner: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract version from config.yaml
        run: |
          VERSION=$(grep '^version:' everything-presence-mmwave-configurator/config.yaml | awk '{print $2}' | tr -d '"')
          MAJOR_VERSION=${VERSION%%.*}
          echo "Extracted version: $VERSION"
          echo "Extracted major version: $MAJOR_VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "MAJOR_VERSION=$MAJOR_VERSION" >> $GITHUB_ENV

      - name: Set Image Tags
        run: |
          echo "IMAGE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:${{ env.VERSION }}" >> $GITHUB_ENV
          echo "LATEST_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:latest" >> $GITHUB_ENV
          echo "MAJOR_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:${{ env.MAJOR_VERSION }}" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./everything-presence-mmwave-configurator
          target: standalone
          platforms: ${{ matrix.platform }}
          push: true
          load: false
          tags: |
            ${{ env.IMAGE_TAG }}-${{ matrix.arch }}
            ${{ env.LATEST_TAG }}-${{ matrix.arch }}
            ${{ env.MAJOR_TAG }}-${{ matrix.arch }}

      - name: Test Docker image
        run: |
          docker run --rm --platform ${{ matrix.platform }} --entrypoint /bin/echo ${{ env.IMAGE_TAG }}-${{ matrix.arch }} "Image built successfully"

  build-push-addon:
    name: Build and Push Add-on Image
    if: github.event_name == 'push'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            arch: amd64
            runner: ubuntu-latest
            build_from: ghcr.io/hassio-addons/base:19.0.0
          - platform: linux/arm64
            arch: arm64
            runner: ubuntu-24.04-arm
            build_from: ghcr.io/hassio-addons/base:19.0.0
          - platform: linux/arm/v7
            arch: armv7
            runner: ubuntu-latest
            build_from: ghcr.io/home-assistant/armv7-base:latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract version from config.yaml
        run: |
          VERSION=$(grep '^version:' everything-presence-mmwave-configurator/config.yaml | awk '{print $2}' | tr -d '"')
          MAJOR_VERSION=${VERSION%%.*}
          echo "Extracted version: $VERSION"
          echo "Extracted major version: $MAJOR_VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "MAJOR_VERSION=$MAJOR_VERSION" >> $GITHUB_ENV

      - name: Set Image Tags
        run: |
          echo "ADDON_IMAGE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon-${{ env.VERSION }}" >> $GITHUB_ENV
          echo "ADDON_LATEST_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon" >> $GITHUB_ENV
          echo "ADDON_MAJOR_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon-${{ env.MAJOR_VERSION }}" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        uses: docker/build-push-action@v4
        with:
          context: ./everything-presence-mmwave-configurator
          target: addon
          platforms: ${{ matrix.platform }}
          push: true
          load: false
          build-args: |
            BUILD_FROM=${{ matrix.build_from }}
          tags: |
            ${{ env.ADDON_IMAGE_TAG }}-${{ matrix.arch }}
            ${{ env.ADDON_LATEST_TAG }}-${{ matrix.arch }}
            ${{ env.ADDON_MAJOR_TAG }}-${{ matrix.arch }}

      - name: Test Docker image
        run: |
          docker run --rm --platform ${{ matrix.platform }} --entrypoint /bin/echo ${{ env.ADDON_IMAGE_TAG }}-${{ matrix.arch }} "Image built successfully"

  manifest-standalone:
    name: Create Standalone Multi-Arch Manifest
    if: github.event_name == 'push'
    needs: build-push-standalone
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract version from config.yaml
        run: |
          VERSION=$(grep '^version:' everything-presence-mmwave-configurator/config.yaml | awk '{print $2}' | tr -d '"')
          MAJOR_VERSION=${VERSION%%.*}
          echo "Extracted version: $VERSION"
          echo "Extracted major version: $MAJOR_VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "MAJOR_VERSION=$MAJOR_VERSION" >> $GITHUB_ENV

      - name: Set Image Tags
        run: |
          echo "IMAGE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:${{ env.VERSION }}" >> $GITHUB_ENV
          echo "LATEST_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:latest" >> $GITHUB_ENV
          echo "MAJOR_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:${{ env.MAJOR_VERSION }}" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create multi-arch manifests
        run: |
          docker buildx imagetools create \
            -t ${{ env.IMAGE_TAG }} \
            ${{ env.IMAGE_TAG }}-amd64 \
            ${{ env.IMAGE_TAG }}-arm64 \
            ${{ env.IMAGE_TAG }}-armv7
          docker buildx imagetools create \
            -t ${{ env.LATEST_TAG }} \
            ${{ env.LATEST_TAG }}-amd64 \
            ${{ env.LATEST_TAG }}-arm64 \
            ${{ env.LATEST_TAG }}-armv7
          docker buildx imagetools create \
            -t ${{ env.MAJOR_TAG }} \
            ${{ env.MAJOR_TAG }}-amd64 \
            ${{ env.MAJOR_TAG }}-arm64 \
            ${{ env.MAJOR_TAG }}-armv7

  manifest-addon:
    name: Create Add-on Multi-Arch Manifest
    if: github.event_name == 'push'
    needs: build-push-addon
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Extract version from config.yaml
        run: |
          VERSION=$(grep '^version:' everything-presence-mmwave-configurator/config.yaml | awk '{print $2}' | tr -d '"')
          MAJOR_VERSION=${VERSION%%.*}
          echo "Extracted version: $VERSION"
          echo "Extracted major version: $MAJOR_VERSION"
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "MAJOR_VERSION=$MAJOR_VERSION" >> $GITHUB_ENV

      - name: Set Image Tags
        run: |
          echo "ADDON_IMAGE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon-${{ env.VERSION }}" >> $GITHUB_ENV
          echo "ADDON_LATEST_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon" >> $GITHUB_ENV
          echo "ADDON_MAJOR_TAG=${{ secrets.DOCKERHUB_USERNAME }}/everything-presence-mmwave-configurator:addon-${{ env.MAJOR_VERSION }}" >> $GITHUB_ENV

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create multi-arch manifests
        run: |
          docker buildx imagetools create \
            -t ${{ env.ADDON_IMAGE_TAG }} \
            ${{ env.ADDON_IMAGE_TAG }}-amd64 \
            ${{ env.ADDON_IMAGE_TAG }}-arm64 \
            ${{ env.ADDON_IMAGE_TAG }}-armv7
          docker buildx imagetools create \
            -t ${{ env.ADDON_LATEST_TAG }} \
            ${{ env.ADDON_LATEST_TAG }}-amd64 \
            ${{ env.ADDON_LATEST_TAG }}-arm64 \
            ${{ env.ADDON_LATEST_TAG }}-armv7
          docker buildx imagetools create \
            -t ${{ env.ADDON_MAJOR_TAG }} \
            ${{ env.ADDON_MAJOR_TAG }}-amd64 \
            ${{ env.ADDON_MAJOR_TAG }}-arm64 \
            ${{ env.ADDON_MAJOR_TAG }}-armv7
